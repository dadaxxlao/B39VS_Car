import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "../components/Header";
import Footer from "../components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "EnviroSecure | Autonomous Hazardous Waste Solutions",
  description: "EnviroSecure provides autonomous robotic systems for safe, efficient, and compliant hazardous waste management in industrial facilities.",
  keywords: "hazardous waste management, autonomous robots, industrial safety, environmental compliance, robotics, Arduino, EnviroSecure",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.className} flex flex-col min-h-screen bg-background text-foreground`}>
        <Header />
        <main className="flex-grow container mx-auto px-4 py-8">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
